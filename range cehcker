#include <stdio.h>

int main() {
    int r, x, y;
    
    scanf("%d %d %d", &x, &y, &r);
    
    if(x*x+y*y>r*r){
        printf("false");
    }
    else{
        printf("true");
    }

    return 0;
}
